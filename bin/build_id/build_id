#!/usr/bin/env bash
#
# Run from the root of your project to detect applicable scripts.
#
set -e
set -o pipefail

if [ -n "${GITHUB_RUN_ID}" ]; then
  build_id_github "$@"
  exit 0
fi

build_id_local
